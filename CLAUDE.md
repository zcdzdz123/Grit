# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 가이드입니다.

## 프로젝트 개요

**Grit Tools** — 설치·로그인 없이 브라우저에서 바로 쓰는 개발자용 미니 도구 모음 (정적 웹사이트).
브랜드명은 **Grit**. 컨셉: "작은 반복을 갈아버리는 도구 모음".

## 핵심 원칙 (매니페스토)

1. 작은 불편도 반복되면 큰 문제다 — 사소한 반복 작업을 해결한다.
2. 설치·로그인·광고 없음 — **도구 페이지에서는 작업에만 집중**한다.
3. **모든 처리는 브라우저 안에서** — 파일·이미지를 서버로 전송하지 않는다 (보안).
4. 불편한 게 생기면 도구가 된다 — 도구를 계속 추가한다.

## 구조

- `index.html` — 랜딩/허브 페이지. 도구 카탈로그. 다크 + 앰버(`#e8a020`) 터미널 감성 디자인. 상단 광고(AdSense) 영역은 허브에만 있음.
- `pixel-coords.html` — 첫 동작 도구(픽셀 좌표 추출기). 새 도구의 단일 파일 템플릿 역할.

## 컨벤션

- **빌드 도구·프레임워크·외부 의존성을 도입하지 않는다.** 순수 HTML/CSS/JS, 파일 더블클릭으로 바로 실행.
- 새 도구는 **단일 HTML 파일**로 만든다 (CSS·JS 인라인).
- 도구 페이지 상단에는 공통 `#grit-bar`(로고 / 도구명 / "← 모든 도구" 링크)를 둔다 — `pixel-coords.html` 참고.
- 구형 브라우저 호환을 위해 ES5 스타일 유지 (`var`, `keyCode`, `execCommand` 폴백 등).
- 도구 페이지에는 광고를 넣지 않는다 (원칙 2).
- 텍스트는 한국어 UI 기준.

## 새 도구 추가 절차

1. `pixel-coords.html`를 템플릿 삼아 새 단일 HTML 파일 생성.
2. `index.html`의 해당 카테고리(Image/Text/File) 카드에서 `soon` 클래스와 `SOON` 배지를 제거하고 `href` 연결, `NEW` 배지 부여.
3. 카테고리 헤더의 `cat-count` 갱신.

## 디자인 토큰 (index.html :root)

- 배경: `--bg0:#0c0c0c` ~ `--bg4:#2a2a2c`
- 강조: `--amber:#e8a020`
- 폰트: IBM Plex Mono (제목/코드), Noto Sans KR (본문) — Google Fonts CDN
