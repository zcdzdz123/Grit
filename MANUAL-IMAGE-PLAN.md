# Grit Tools 기능별 이미지 매뉴얼 계획

공통 그림을 재사용하지 않고 각 도구를 실제로 실행한 화면만 사용한다. 이미지는 입력값 자체보다 도구가 해결하는 문제와 결과를 한눈에 이해할 수 있는 장면을 우선한다.

## 장면 선정 원칙

- 계산기: 결과 숫자와 함께 그래프, 기간 차이, 공제 내역 등 판단 근거를 보여준다.
- 이미지 도구: 픽셀 경계, 색상값, 원본 대비 크기·용량처럼 시각적 목적을 명확히 보여준다.
- 텍스트 도구: 입력과 변환 결과를 한 화면에서 비교한다.
- 파일·PDF 도구: 빈 업로드 화면 대신 실제 파일명, 순서, 시트, 페이지 범위와 생성 결과를 보여준다.
- 예제 데이터에는 개인정보를 사용하지 않는다.
- 최종 화면은 WebP로 저장하고 각 페이지의 상세 설명 바로 앞에 배치한다.

## 현재 대표 장면

| 도구 | 대표 장면 |
|---|---|
| 복리 계산기 | 잔액이 곡선으로 증가하는 회차별 결과 |
| 단리 계산기 | 같은 이자가 더해지는 직선형 그래프 |
| 환율 계산기 | 100 USD의 KRW 변환과 주요 통화 비교 |
| D-Day 계산기 | 연말 목표일까지 남은 일수 |
| 연봉 실수령액 | 4대보험·세금 공제에서 월 실수령액까지 |
| 좌표 추출기 | 서로 다른 색이 만나는 경계 픽셀의 좌표 |
| 색상 추출기 | 경계 픽셀의 HEX·RGB·HSL 값 |
| 이미지 리사이즈 | 원본과 축소 후 해상도·용량 비교 |
| 주소 영문 변환 | 영문 주소와 구성 요소 분리 |
| 표→마크다운 | 붙여넣은 표, 코드, 렌더링 결과 비교 |
| 텍스트 병합 | 파일 순서와 헤더가 반영된 병합 미리보기 |
| 엑셀 시트 분리 | 매출·비용·요약 시트 선택 |
| 파일명 일괄 변경 | 원래 이름과 새 이름의 대조표 |
| PDF 합치기 | 표지·본문·부록의 순서와 총 페이지 수 |
| PDF 나누기 | 6쪽 문서를 2쪽씩 범위 분할한 결과 |
| PDF→이미지 | 페이지별 JPEG 미리보기 |

재현용 예제 파일은 `manual-fixtures/`, 최종 이미지는 `assets/manual/<tool-slug>/`에 둔다.
